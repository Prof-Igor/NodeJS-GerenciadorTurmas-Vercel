// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgres://Prof-Igor:sZRVmk2O1NKo@ep-noisy-lab-13800049.us-east-2.aws.neon.tech/turmas_db")
}

model Book {
  id          String @id @default(uuid())
  name        String
  description String

  @@map("books")
}
